export interface ShapeConfig {

  /* Gap of the shape */
  gap: number;

  /* Size of the shape */
  size: number;

  /* Number of steps before shape completion */
  steps: number;

  /* Radius of the shape */
  radius: number;

  /* Center position of the shape */
  center: number;

  /* Color of the shape */
  color: string;

}
